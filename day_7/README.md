# day_7 (first_task) (#exam eligibility checker)

ohh god..😵‍💫,this was the first code among all those which are written by me in which i was literally got frustrated because there were a lot of small mistakes and confusions during writting of code.But at the end, i got realised that it was just a game of pure logic based thinking.
anyways,this is my first task for the day_7 of this python coding journey. i literally got frustrated to run this code correctly and finally i did it🎉.although, i had taken a little bit help from gemeni but this question made me realise that i can solve these kind of logical problems just by keep my mind kalm and focused.
let's take a look over the features of this device:
1. Pure Boolean Logic (No Conditionals): Evaluates student exam eligibility strictly using logical expressions (or) and boolean values, eliminating the overhead of traditional if-else conditional blocks.
2. Case-Insensitive Input Handling: Implements the .lower() string method to standardize user inputs. This ensures inputs like "Yes", "YES", or "yes" are processed uniformly without breaking the logic.
3. Multi-Format Input Acceptance: The medical certificate evaluation criteria flexibly checks and validates both short-form (y) and long-form (yes) affirmative responses seamlessly.

# day_7 (second_task) (loan eligibility approval)

Here's the second task of today.it is a device which basically takes decisions that weather a person is eligible to take loan or not on the given criteria in the code.some features of this code are as follows:
1. Smart Boolean Logic (No if-else): Evaluates loan eligibility strictly using Python's logical or operator, showcasing clean and optimized code without unnecessary conditional branching.
2. Dual-Criteria Evaluation: Automatically checks if a applicant meets at least one of the bank's key conditions:
​Monthly Income > 50,000
​Credit Score \ge 750
Clean & Readable Output: Utilizes built-in string formatting methods like .center() and strategic spacing to ensure a neat, user-friendly terminal interface.

# day_7 (third_task) (#movie theater entry)
Here's the third task for today. this is a device which detects that weather a viewer is elegible or not to go in the movie theater. this code and the second one are for the practice purpose of the first code concepts. 
here are some of the main characteristics of this code:
1. Strict Logical Evaluation (and Operator): Ensures entry is granted exclusively when both age and ticket requirements are satisfied simultaneously, mirroring real-world theater rules.
2. Case-Insensitive Input Handling: Applies the .lower() method to the ticket response, making the system robust against different user typing styles (e.g., "Yes", "YES", or "yes").
3. Efficient No-Condition Logic: Successfully fulfills the challenge requirements by computing the final result using pure boolean algebra, eliminating the need for bulky if-else blocks.
4. Clean Terminal Presentation: Utilizes formatting methods like .center() alongside deliberate spacing (print()) to output a neat and highly readable CLI layout.
